services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    command: ["uvicorn", "apps.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
    ports:
      - "8000:8000"
